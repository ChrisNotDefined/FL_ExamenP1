import 'package:flutter/material.dart';
import 'package:flutter_examen_p1/src/pages/main_page.dart';

class MyApp extends StatelessWidget {
  Widget build(context) {
    return MaterialApp(
      home: Center(
        child: MainPage(),
      ),
    );
  }
}